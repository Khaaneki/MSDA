<h1>Bonjour {{ context.email.toNomComplet }}!</h1>

    <p>
        Vous êtes connecté en tant que : {{ context.email.to[0].address }}
    </p>
    
    <p>
        Nom / Prénom: {{ context.contact.getNomComplet() }}
    </p>
    
    <p>
        Email : {{ context.contact.getEmail() }}
    </p>
    
    <p>
        Sujet : {{ context.contact.getSujet() }}
    </p>
    
    <p>
        Demande : {{ context.contact.getMessage() }}
    </p>
    
    <a href="#">Click here to activate your account</a>
    (this link is valid until {{ context.expiration_date|date('F jS') }})
    